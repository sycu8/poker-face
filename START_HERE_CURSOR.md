# Start here (Cursor)

1. Read `README.md`, `docs/ROADMAP.md`, `docs/GAME_RULES.md`, `docs/BRAND_GUIDE.md`, and `.cursor/rules/poker-faces.md`.
2. Copy `.env.example` → `.dev.vars` and set `SESSION_SECRET`.
3. `npm install && npm run db:migrate:local && npm run dev`
4. Implement in the required phase order from the master prompt. Do not skip failing correctness tests.
5. Production deploy only after the production gate report and explicit approval.
